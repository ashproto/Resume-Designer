### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

### VS Code
# VSCode settings (keep shared configuration)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

# Local brainstorm / visual-companion scratch dir
.superpowers/

# Claude session-memory plugin state (machine-local, regenerated)
.remember/

# Stray root-level dependency/cache dirs (the app's real node_modules lives in
# resume-designer/ and is ignored there; tooling occasionally writes a cache here)
/node_modules/

# Generated Chrome Web Store packages
/extension/artifacts/

# Temporary credentials created by google-github-actions/auth if its defaults change
gha-creds-*.json
